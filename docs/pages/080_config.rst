Config
------

The LYTConfig class allows you to set global config for the library. Most of these properties can also be set individually on tests. If they are set in both places, the test's methods are used.
