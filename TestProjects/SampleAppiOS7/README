This project takes an iOS 7 project which does not use 'use_frameworks!' in the Podfile. This was a bug at one point (see issue #8), so we should ensure that this project builds whenever we release.
Tests should build and pass.

